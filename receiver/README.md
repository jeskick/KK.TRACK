# 接收端 (RX)

固件：[`pio/rx/`](pio/rx/)（BLE + WiFi AP + PPM + 网页）

```powershell
cd receiver\pio\rx
pio run -t upload --upload-port COMxx
```

见 [../docs/BUILD.md](../docs/BUILD.md)、[../docs/USAGE.md](../docs/USAGE.md)。
