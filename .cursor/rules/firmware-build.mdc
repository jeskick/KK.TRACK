---
description: RX/TX 固件编译时自动 bump 版本并更新 ota/*.bin
globs: receiver/pio/rx/**,transmitter/pio/tx/**,tools/pio_fw_version.py,ota/**
alwaysApply: false
---

# 固件版本与 OTA 包

每次 **`pio run`**（RX 或 TX）会自动：

1. 将对应 `CMakeLists.txt` 里 `project(... VERSION "x.y.z")` 的 **patch +1**
2. 编译成功后复制 `firmware.bin` → `ota/RX.x.y.z.bin` 或 `ota/TX.x.y.z.bin`

实现：`tools/pio_fw_version.py`（通过各 `platformio.ini` 的 `pre:` 挂载）。

## 约定

- RX 版本：`receiver/pio/rx/CMakeLists.txt`
- TX 版本：`transmitter/pio/tx/CMakeLists.txt`
- OTA 包命名：**`RX.1.0.9.bin`** / **`TX.1.0.8.bin`**
- 网页 OTA：先 RX 后 TX；TX 中继需 BLE 已连
- USB 全片烧录：`flash_rx.ps1` / `flash_tx.ps1`

## 构建变体

- `pio run` → 调试版（WARN 日志）
- `pio run -e esp32-c3-release` → 正式版（无日志）

## 代理须知

- 改完固件后帮用户编译时，**不要手改版本号**（pre 脚本会 bump）
- 详细烧录说明在用户本地 `docs/BUILD.md`（不上传 Git）
